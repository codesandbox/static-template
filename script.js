/*const cube = document.querySelector("#cube");
const makeDiv = (width, height, depth) => {
  const a = document.createElement("div");
  const sheet=a.sheet;

  return a;
};
let width = 200,
  height = 200,
  depth = 200;
const front = makeDiv(200, 200, 200);
*/
