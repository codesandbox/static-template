let esVerdadero = false;
let resultado = !esVerdadero;

if (resultado) {
  alert("El resultado es true.");
} else {
  alert("El resultado es false.");
}

// Primero inicializamos una variable booleana esVerdadero con el valor false.
// Luego, utilizamos el operador lógico ! para negar el valor de esVerdadero
// y almacenamos el resultado en la variable resultado.
