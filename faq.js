/*

let x = 0;
let rotated = false;

$(".question-holder").click(function () {
  $(".open-question").removeClass("selected");
  $(this).find("img.open-question").addClass("selected");
  $(".open-question:not(.selected)").css("transform", "rotate(0deg)");

  if (rotated == false) {
    $(this).find("img.open-question").css("transform", "rotate(45deg)");
    rotated = true;
  } else if (rotated == true) {
    $(this).find("img.open-question").css("transform", "rotate(0deg)");
    rotated = false;
  }
});

*/
